# ==============================================================================
# OpenQuatt - Duo Topology Main Config (Heatpump Listener)
# ==============================================================================
substitutions:
  <<: !include openquatt/oq_substitutions_common.yaml
  <<: !include openquatt/profiles/oq_substitutions_heatpump_listener.yaml

esphome:
  build_path: build/openquatt_duo_heatpump_listener

packages:
  openquatt_base: !include openquatt_base.yaml
  oq_heatpump_listener_extras: !include openquatt/profiles/oq_heatpump_listener_extras.yaml
