# ==============================================================================
# OpenQuatt - Shared Base Config (Single topology)
# ==============================================================================
# Common ESPHome/ESP32 runtime shared by all hardware profiles.
# Hardware-specific pin/board substitutions are provided by profile files.
# ==============================================================================

# ==============================================================================
# PACKAGES
# ==============================================================================
substitutions:
  <<: !include openquatt/topology/oq_topology_single.yaml
  oq_topology_build_flag: "-DOQ_TOPOLOGY_DUO=0"
  main_release_manifest_url: "https://github.com/jeroen85/OpenQuatt/releases/latest/download/openquatt-single-ota.manifest.json"
  dev_release_manifest_url: "https://github.com/jeroen85/OpenQuatt/releases/download/dev-latest/openquatt-single-ota.manifest.json"
  # Stable OTA channel by default. Dev builds should override both substitutions
  # via `esphome -s release_channel dev -s release_manifest_url ... compile ...`.
  release_manifest_url: "https://github.com/jeroen85/OpenQuatt/releases/latest/download/openquatt-single-ota.manifest.json"

packages:
  base_common: !include openquatt_base_common.yaml
  <<: !include openquatt/oq_packages_common.yaml
