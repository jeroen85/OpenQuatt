# ==============================================================================
# OpenQuatt - Single Topology Main Config (Waveshare ESP32-S3-Relay-1CH)
# ==============================================================================
substitutions:
  <<: !include openquatt/oq_substitutions_common.yaml
  <<: !include openquatt/profiles/oq_substitutions_waveshare.yaml

esphome:
  build_path: build/openquatt_single_waveshare

packages:
  oq_waveshare_psram: !include openquatt/profiles/oq_psram_waveshare.yaml
  openquatt_base: !include openquatt_base_single.yaml
