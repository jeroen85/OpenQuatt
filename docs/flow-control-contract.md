# Flow Control Contract (Archived)

The detailed flow-control contract is archived in local reference notes:
`docs/_local_reference/contracts/flow-control-contract.md`.

The active public contract is:
`docs/control-contract.md`.
