# Thermal Contract (Archived)

The detailed thermal contract is archived in local reference notes:
`docs/_local_reference/contracts/thermal-contract.md`.

The active public contract is:
`docs/control-contract.md`.
