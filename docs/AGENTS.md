# Docs

- Keep docs user-facing, concise, and in the existing Dutch tone unless the task says otherwise.
- Read only linked pages needed for the edit; avoid `_local_reference/` unless explicitly requested.
- Preserve relative links and dashboard/install structure.
- For consistency changes, prefer targeted scripts such as `rtk python3 scripts/check_docs_consistency.py`.
